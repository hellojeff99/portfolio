---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className } = Astro.props;
---

<h3
  title={text}
  class:list={[
    'min-w-0 truncate text-xs font-semibold tracking-tight text-slate-900',
    className,
  ]}
>
  {text}
</h3>
