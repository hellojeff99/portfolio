---
import SectionHead from "./SectionHead.astro";

interface Props {
  title: string;
  id: string;
}

const { title, id } = Astro.props;
---

<section class="px-12 py-2">
  <SectionHead {title} {id} />
  <div>
    <slot />
  </div>
</section>
