---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className } = Astro.props;
---
<p class:list={[
    "min-w-0 truncate text-xs font-medium text-slate-500",
    className,
  ]}>
  {text}
</p>
